apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-workshop-test.fullname" . }}
  labels:
    {{- include "helm-workshop-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-workshop-test.selectorLabels" . | nindent 4 }}
