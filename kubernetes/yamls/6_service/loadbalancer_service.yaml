apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: deboeck
  labels:
    owner: deboeck
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 9376
      targetPort: 80
