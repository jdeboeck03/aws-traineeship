apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: deboeck
  labels:
    owner: deboeck
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
      nodePort: 30007
